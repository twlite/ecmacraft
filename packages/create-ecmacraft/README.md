# `create-ecmacraft`

A CLI tool to generate ecmacraft plugin template.

## Usage

```bash
pnpm create ecmacraft <plugin-name>
```

This will create a new directory with the specified plugin name, containing a basic ecmacraft plugin template. You can then navigate to the directory and start developing your plugin.
